# Check Script Global Leak

checks if a script has variables set to global context (window)

# Run

It runs a local express.js server at 8080.

### First time

`npm install`

### Otherwise

`npm run server`